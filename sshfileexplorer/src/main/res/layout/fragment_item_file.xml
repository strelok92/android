<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:backgroundTint="@color/dialog_entry"
    >

    <LinearLayout
        style="@style/item_file_frame"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/iFolder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone"
            app:srcCompat="@drawable/baseline_folder_32" />

        <ImageView
            android:id="@+id/iFile"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone"
            app:srcCompat="@drawable/baseline_text_snippet_32" />

        <LinearLayout
            style="@style/item_file_frame"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tFileName"
                style="@style/item_file_name" />
            <TextView
                android:id="@+id/tFileSize"
                style="@style/item_file_info" />

        </LinearLayout>
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageButton
            android:id="@+id/bFileDownload"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:srcCompat="@drawable/baseline_download_32"
            android:layout_gravity="right|center"
            android:layout_margin="10dp"
            android:backgroundTint="@color/item_button"
            />

    </FrameLayout>

</FrameLayout>
